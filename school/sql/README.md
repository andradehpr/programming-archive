# SQL

SQL Server scripts — table creation, queries, relational models, and transaction examples.

Files include an ATM schema (`atm.sql`), student queries, and additional classroom exercises.

Run scripts in SQL Server Management Studio or `sqlcmd`.
